import { HelpSupport } from "@/user/client/helpSupport"
import Layout from "../layout/Layout"

export default function ClientSupport() {

  return (
    <Layout>
      <HelpSupport />
    </Layout>
  )
}
